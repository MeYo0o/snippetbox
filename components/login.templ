package components

templ Login(form UserLoginForm, flash string, isAuthenticated bool) {
	@Base("Login", loginBody(form), flash, isAuthenticated)
}

templ loginBody(form UserLoginForm) {
	<form action="/user/login" method="POST" novalidate>
		for _, err := range form.NonFieldErrors {
			<div class="error">{ err }</div>
		}
		<div>
			<label>Email:</label>
			if emailErr, ok:= form.FieldErrors["email"];ok {
				<label class="error">{ emailErr }</label>
			}
			<input type="email" name="email" value={ form.Email }/>
		</div>
		<div>
			<label>Password:</label>
			if passwordErr, ok:= form.FieldErrors["password"];ok {
				<label class="error">{ passwordErr }</label>
			}
			<input type="password" name="password"/>
		</div>
		<div>
			<input type="submit" value="login"/>
		</div>
	</form>
}
